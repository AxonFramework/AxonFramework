# Kotlin Extension - TODO

This folder (as `/stash`) keeps the files from the original, external [`extension-kotlin`](https://github.com/AxonFramework/extension-kotlin) repository. 

We will migrate files to the new AF5 based internal extension or remove them, if not applicable anymore.
