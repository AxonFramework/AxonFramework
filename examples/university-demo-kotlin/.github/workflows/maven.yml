name: 🚀 Java CI with Maven

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        axon-framework:
          - version: 5.0.0-RC3
          - version: 5.0.0-SNAPSHOT
      fail-fast: false

    steps:
      - name: 🛎️ Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version-file: .java-version
          distribution: 'zulu'
          cache: maven

      - name: 📦 Build with Maven
        run: mvn -B compile -ntp -B -Daxon.version=${{ matrix.axon-framework.version }}

      - name: 🧪 Run tests
        run: mvn -B test -ntp -B
