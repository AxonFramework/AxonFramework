spring:
  docker:
    compose:
      file: file:examples/docker-compose.yaml
  main:
    web-application-type: reactive
axon:
  server:
    enabled: true

springdoc:
  swagger-ui:
    try-it-out-enabled: true
